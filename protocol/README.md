# Protocol

This directory contains protocol specifications and shared transfer contract
documentation for BeamDrop.

The protocol should define device identity, pairing, discovery, transfer
metadata, content framing, integrity checks, encryption expectations, and version
compatibility.
