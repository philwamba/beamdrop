# BeamDrop Relay

This directory is reserved for the optional BeamDrop relay service.

The relay service may help devices transfer data when direct local paths are not
available, but it should not become required cloud storage for the local MVP.
