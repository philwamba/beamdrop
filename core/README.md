# Core

This directory contains shared BeamDrop core logic.

Core code should be used for behavior that benefits from one correct
implementation across platforms, such as transfer state machines, cryptographic
helpers, validation, protocol bindings, and reusable networking primitives.
