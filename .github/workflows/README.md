# Workflows

This directory is reserved for GitHub Actions workflows.

No workflows have been added yet. Future workflows should validate native app,
shared core, protocol, and server changes without introducing web-only build
assumptions.
