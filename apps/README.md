# Apps

This directory contains native BeamDrop applications for supported platforms.

Each app should use the platform's native UI stack and OS integration model. Do
not add Electron, Tauri, Flutter, React Native, Ionic, Cordova, web wrappers, or
browser-only app architecture here.
